language: php
php:
  - 7.2
  - 7.3
  - 'nightly'

before_script:
  - composer install
  - echo $'apc.enabled=1\napc.enable_cli=1' >> ~/.phpenv/versions/$(phpenv 5.1)/etc/conf.d/travis.ini

script: vendor/bin/phpunit --coverage-clover=coverage.xml

after_success:
  - bash <(curl -s https://codecov.io/bash)